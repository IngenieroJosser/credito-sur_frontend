'use client'

/**
 * Redirige al coordinador a la vista de revisiones centralizada en /admin/revisiones
 * usando el layout del coordinador.
 */
import RevisionesPage from '@/app/admin/revisiones/page'

export default function CoordinadorRevisionesPage() {
  return <RevisionesPage />
}
